export interface CartItemInterface {
  id: string;
  label: string;
  price: number;
  rating: number;
  quantity: number;
}

export interface FoodItemInterface {
  label: string;
  price: number;
  image: string;
  rating: number;
}
